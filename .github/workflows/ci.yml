name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ekos/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('ekos/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build (all crates)
        working-directory: ekos
        run: cargo build --workspace --locked

      - name: Test (unit + integration)
        working-directory: ekos
        run: cargo test --workspace

      - name: Clippy (no warnings, including test code)
        working-directory: ekos
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Format check
        working-directory: ekos
        run: cargo fmt --check

  integration:
    name: Integration tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            tests/integration/target
          key: ${{ runner.os }}-cargo-integration-${{ hashFiles('tests/integration/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-integration-

      - name: Test
        working-directory: tests/integration
        run: cargo test

      - name: Clippy (no warnings, including test code)
        working-directory: tests/integration
        run: cargo clippy --all-targets -- -D warnings

      - name: Format check
        working-directory: tests/integration
        run: cargo fmt --check

  web:
    name: Web console
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable

      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ekos/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('ekos/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build ekos (for the live MCP-client test)
        working-directory: ekos
        run: cargo build --release -p ekos --locked

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          enable-cache: true

      - name: Sync API deps
        working-directory: web/api
        # No --no-build here: this sync installs the project's own editable package (app/), which
        # requires a local build step that isn't a supply-chain concern — it's our own source, not
        # a third-party sdist. --frozen already pins every dependency to the reviewed lockfile.
        run: uv sync --frozen

      - name: Ruff (lint + format)
        working-directory: web/api
        run: |
          uv run --no-sync --no-build ruff check .
          uv run --no-sync --no-build ruff format --check .

      - name: Pytest (unit + live MCP)
        working-directory: web/api
        run: uv run --no-sync --no-build pytest -q
        env:
          EKOS_BIN: ${{ github.workspace }}/ekos/target/release/ekos

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/ui/package-lock.json

      - name: Install UI deps
        working-directory: web/ui
        # --ignore-scripts: verified locally that this dependency tree's only lifecycle script
        # (esbuild's postinstall) isn't needed — esbuild resolves its platform binary through a
        # normal optionalDependencies package, not the script, so the build still succeeds.
        run: npm ci --ignore-scripts

      - name: Test UI
        working-directory: web/ui
        run: npm test

      - name: Typecheck + build UI
        working-directory: web/ui
        run: |
          npm run typecheck
          npm run build

  benchmark:
    name: Benchmark
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable

      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            benchmark/target
          key: ${{ runner.os }}-cargo-bench-${{ hashFiles('benchmark/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-bench-

      - name: Run benchmarks
        working-directory: benchmark
        run: cargo bench

      - name: Upload benchmark report
        uses: actions/upload-artifact@v7
        with:
          name: criterion-report
          path: benchmark/target/criterion
