diff --git a/ekos/crates/recovery/src/statement_repair.rs b/ekos/crates/recovery/src/statement_repair.rs
index 471fcc4..95a78ec 100644
--- a/ekos/crates/recovery/src/statement_repair.rs
+++ b/ekos/crates/recovery/src/statement_repair.rs
@@ -69,6 +69,7 @@ fn ends_with_set_op_keyword(trimmed: &str) -> bool {
 
 fn starts_with_keyword(trimmed: &str, kw: &str) -> bool {
     trimmed.len() >= kw.len()
+        && trimmed.is_char_boundary(kw.len())
         && trimmed[..kw.len()].eq_ignore_ascii_case(kw)
         && trimmed[kw.len()..]
             .chars()
