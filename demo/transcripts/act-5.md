# Act 5 transcript

**Prompt:** Remember this for future sessions: headless.sh's act filter used to compare the target act number against itself instead of against the requested list, so passing one act number silently ran all of them — always verify a filter function against a case where it should say no.

```
Saved. Noted for future sessions: `headless.sh`'s act filter had a self-comparison bug that made it match everything, and the general lesson to always test filters against a rejection case.
```
